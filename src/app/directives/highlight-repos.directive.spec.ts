import { HighlightReposDirective } from './highlight-repos.directive';

describe('HighlightReposDirective', () => {
  it('should create an instance', () => {
    // const directive = new HighlightReposDirective();
    // expect(directive).toBeTruthy();
  });
});
